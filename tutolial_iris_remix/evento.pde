void keyPressed() {
  // Alterna el valor de isFade cuando se presiona 'f' o 'F'
  if (key == 'f' || key == 'F') {
    isFade = !isFade;
  }
  
  // Alterna el valor de isCirculo cuando se presiona 'c' o 'C'
  if (key == 'c' || key == 'C') {
    isCirculo = !isCirculo;
  }
}

// if (key == 'c' || key == 'C') {
//   isCirculo =+ isCirculo;
// dues barres || vol dir "or"
